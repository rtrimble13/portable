# Runtime dependencies for `portable`.
#
# Pinned in constraints.txt; this file states intent, that file states the
# resolved tree. Install with:
#     pip install -r requirements.txt -c constraints.txt
typer>=0.12
rich>=13.7
